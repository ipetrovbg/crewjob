(function(){    var app = angular.module('crewjob');    var limitText = function() {        return function(input, limit, pref) {            if (input) {                if (limit > input.length) {                    return input.slice(0, limit);                } else {                    if(pref){                        return input.slice(0, limit) + pref;                    }else{                        return input.slice(0, limit);                    }                }            }        }    };    app.filter('limitText', limitText);}());